# 数学分析培优讲义 LaTeX 工程

- 总书编译：在项目根目录编译 `数学分析培优讲义.tex`。
- 单章编译：直接打开 `章节/第X章_*.tex` 编译；每章通过 `subfiles` 继承总书导言区，并预设正确章号。
- 分页规则：`\chapter` 自身换页；第一个紧随章标题的层级标题不额外换页。之后新的 `section` / `subsection` / `methodsection` 若前面已有正文，则在标题前插入 `\newpage`；连续嵌套标题之间不重复换页，避免出现只有标题的大面积空白页。
- 例题后统一通过 `\exampleblank` 留出书写空间。
