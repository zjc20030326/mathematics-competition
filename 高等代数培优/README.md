# 高等代数培优讲义

本目录按照 `数学分析培优/` 的工程格式完成高等代数培优讲义的 LaTeX 工程化整理。

## 当前状态

原《高代培优上打印版》《高代培优下打印版》的十章内容已经全部纳入当前项目：

```text
第一章   多项式
第二章   行列式
第三章   矩阵
第四章   线性空间与线性方程组
第五章   线性变换
第六章   相似标准型
第七章   二次型
第八章   欧氏空间
第九章   矩阵综合
第十章   张量积与外积
```

目前共整理 **511 道例题**，普通版与答案版均由同一份章节内容源码生成。

其中第十章《张量积与外积》是原讲义的选学章节，内容包括：

```text
10.1 张量积
    张量积的构造
    万有性
    维数与基
    对偶与自然同构
    Kronecker 积
10.2 外积
    交错多线性映射
    外积空间
    外积的万有性
    外积空间的维数与基
    与行列式的联系
```

第十章共整理 **9 道例题 + 9 个 `solution`**。

## 工程结构

```text
高等代数培优/
├─ elegantbook.cls
├─ preamble.tex
├─ README.md
├─ 解答问题记录.md
├─ 高等代数培优讲义.tex
├─ 高等代数培优讲义(答案版).tex
└─ 章节/
   ├─ 01_第一章_多项式/
   ├─ 02_第二章_行列式/
   ├─ 03_第三章_矩阵/
   ├─ 04_第四章_线性空间与线性方程组/
   ├─ 05_第五章_线性变换/
   ├─ 06_第六章_相似标准型/
   ├─ 07_第七章_二次型/
   ├─ 08_第八章_欧氏空间/
   ├─ 09_第九章_矩阵综合/
   └─ 10_第十章_张量积与外积/
```

每章采用三文件单源结构：

```text
第X章_章名/
├─ 第X章_章名_内容.tex
├─ 第X章_章名.tex
└─ 第X章_章名(答案版).tex
```

其中 `_内容.tex` 是唯一真正维护理论正文、题目与解答的文件。

## 普通版与答案版

普通版：

```text
完整理论正文 + 例题 + 书写留白
```

答案版：

```text
完整理论正文 + 例题 + 解答
```

`solution` 的显示由 ElegantBook 的 `result=answer/noanswer` 机制统一控制，
不维护两套数学正文。

## 编译

推荐环境：

```text
Windows
TeX Live
VS Code
LaTeX Workshop
XeLaTeX
latexmk
```

普通版：

```powershell
latexmk -xelatex "高等代数培优讲义.tex"
```

答案版：

```powershell
latexmk -xelatex "高等代数培优讲义(答案版).tex"
```

各章普通版、答案版入口也都支持独立编译。

## 字体、中文强调与标点

字体机制保持 ElegantBook 原样:定理类环境与 `solution`、`note` 等楷体正文环境用
`\citshape`(楷体),`proof` 用 `\cfs`(仿宋),`remark` 不切字体,`example` 等用宋体;
没有对 `zhkai` / `zhfs` 做任何 `BoldFont` 补丁.

楷体/仿宋正文里的中文强调统一用 `preamble.tex` 提供的 `\strongcn`:

```latex
\newcommand{\strongcn}[1]{{\cbfseries #1}}
```

`\cbfseries` 在中文环境下等于 `\heiti`,所以这类正文里写 `\strongcn{...}`,
不要写 `\textbf{...}`(楷体/仿宋没有声明粗体字重,`\textbf` 会退回伪粗体并报警).
宋体正文里 `\textbf{...}` 照常可用;ElegantBook 环境标题自身的 `\textbf` 不要改.

章节正文的自然语言使用 ASCII 标点(`,`、`.`、`;`、`:`、`()`、`` ``…'' ``、`--`),
不使用全角标点(如 `，`、`。`、`；`、`：`、`（）`、`“”`、`、`、`—`、`…`),
与 validator 的 `PUNC001` 一致;`$...$` 与数学环境内不受影响,省略号仍用
`\ldots` / `\cdots`.

## 解答问题记录

`解答问题记录.md` 专门保存整理过程中发现的：

- 原题题面错误或疑义；
- 条件不足；
- 符号、下标或印刷错误；
- 原讲义定理表述需要补充条件的地方；
- 仍值得进行第二轮数学复核的解答。

这些问题没有被静默修改。正文中若必须采用标准结论，会明确指出原题问题，
并在 `解答问题记录.md` 中留下对应记录。

## 后续工作

十章的第一轮整理已经完成。后续主要是质量校勘，而不是继续补章：

```text
逐章数学二审
疑问题面对照原始 PDF 校勘
必要时补充更详细的证明
个别版面与公式断行优化
```
